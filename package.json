{
  "name": "bdask",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && yarn start",
    "build": "cd frontend && yarn build",
    "start": "cd frontend && yarn start"
  }
}
